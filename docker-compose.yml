
services:
  backend:
    build: ./blogy_backend
    ports:
      - "8000:8000"
    env_file:
      - ./blogy_backend/.env

  frontend:
    build: ./blogy_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend